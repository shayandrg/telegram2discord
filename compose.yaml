services:
  node:
      image: node:18
      ports:
          - '10808:10808'
      volumes:
          - '.:/srv'
      working_dir: /srv
      command: "npm install && prisma generate && npm run start"
# extra_hosts:
#     - "host.docker.internal:host-gateway"
